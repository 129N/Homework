public class Trash {


/*
    public static boolean if_exist(String Target, DataStorage<Data>Pool){ // Daily dataのIDがに存在するか？
        boolean found = false;

        for(int i = 0; i < Pool.size(); i++){
            if (Target == Pool.get(i).getIdentifier()) {
                found = true;
                break;
            }
        }
        return found;
    }

    public static int Search_Index (String Target, DataStorage<Data>Pool){ // Daily dataのIDがどこに存在するか？
        int Index = 0;

        for(int i = 0; i < Pool.size(); i++){
            if (Target == Pool.get(i).getIdentifier()) {
                Index = i;
                break;
            }
        }
        return Index;

    }
 */


}
